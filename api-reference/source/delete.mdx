---
title: 'Delete Source'
api: 'DELETE /source/:_id'
description: 'Delete a source'
authMethod: "bearer"
---

This endpoint deletes (archives) a source for the authenticated user's account and removes associated vectors.

### Path Parameters

<ParamField path="_id" type="string" required>
  The ID of the source to be deleted.
</ParamField>

### Response

<ResponseField name="message" type="string">
  A success message indicating the deletion was successful.
</ResponseField>

<RequestExample>
```bash
curl -X DELETE 'https://api.privvy.xyz/source/60a3e5b9f3f3a2001f3e8d1a' \
-H 'Authorization: Bearer YOUR_API_KEY'
```
</RequestExample>

<ResponseExample>
```json
{
  "message": "Deletion successful"
}
```
</ResponseExample>

### Error Responses

<ResponseField name="error" type="string">
  Error message if the source deletion fails.
</ResponseField>

- HTTP 404: Returned if the source is not found.
- HTTP 500: Returned if there's a server error during source deletion.

<Note>
  This operation archives the source by setting `isArchived` to true and deletes related vectors.
</Note>